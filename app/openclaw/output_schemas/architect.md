Expected output schema for Architect.

Return a concise JSON object with:
- `status`: `approved` or `rejected`
- `summary`: short technical decision
- `decision`: approval rationale or rejection rationale
- `next_action`: `task:backlog` or `draft_rejected`
