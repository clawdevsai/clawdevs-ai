# SECURITY_TEST_CASES.md - dev_backend

minimum_checks:
  - input validation and unsafe payload handling
  - authentication/authorization boundaries
  - secret handling and leak prevention
  - dependency risk baseline (when applicable)
  - evidence logging for failures and mitigations
