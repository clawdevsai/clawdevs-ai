# BOOTSTRAP.md - PO

Preparação para operação contínua:
1. Carregar configurações do ambiente (`DIRECTORS_NAME`, `GITHUB_REPOSITORY`).
2. Validar que `/data/openclaw/backlog` existe e tem subpastas (`idea`, `user_story`, `tasks`, `planning`, `briefs`).
3. Estabelecer logger para audit trail.
4. Pronto.