# DevOps worker: consome event:devops (após merge), seta estado Deployed.
# Requer ConfigMap devops-worker-scripts (devops_worker.py, gpu_lock.py, issue_state.py).
# GitHub: SSH key montada via initContainer a partir do Secret clawdevs-github-secret;
#         fallback HTTPS via GITHUB_TOKEN (também no Secret).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-worker
  namespace: ai-agents
  labels:
    app: devops-worker
spec:
  replicas: 0
  selector:
    matchLabels:
      app: devops-worker
  template:
    metadata:
      labels:
        app: devops-worker
    spec:
      serviceAccountName: devops-admin-sa
      initContainers:
        # Monta a chave SSH privada do Secret em /ssh-out/ com permissões corretas.
        # O volume emptyDir compartilhado é depois montado em /root/.ssh do container principal.
        - name: setup-ssh
          image: alpine:3.19
          command:
            - /bin/sh
            - -c
            - |
              set -e
              mkdir -p /ssh-out
              chmod 700 /ssh-out
              SSH_KEY="${GITHUB_SSH_KEY_NAME:-id_ed25519}"
              if [ -n "$GITHUB_SSH_PRIVATE_KEY" ]; then
                echo "$GITHUB_SSH_PRIVATE_KEY" | base64 -d > "/ssh-out/$SSH_KEY"
                chmod 600 "/ssh-out/$SSH_KEY"
              fi
              cat > /ssh-out/config <<'SSHEOF'
              Host github.com
                HostName github.com
                User git
                IdentityFile /root/.ssh/$(GITHUB_SSH_KEY_NAME)
                IdentitiesOnly yes
                StrictHostKeyChecking no
              SSHEOF
              chmod 600 /ssh-out/config
          env:
            - name: GITHUB_SSH_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  name: clawdevs-github-secret
                  key: GITHUB_SSH_PRIVATE_KEY
                  optional: true
          volumeMounts:
            - name: ssh-setup
              mountPath: /ssh-out
          resources:
            requests:
              memory: "32Mi"
              cpu: "20m"
            limits:
              memory: "64Mi"
              cpu: "100m"
      containers:
        - name: worker
          image: python:3.12-slim
          command:
            - /bin/sh
            - -c
            - "pip install -q redis && cd /config && exec python devops_worker.py"
          envFrom:
            - secretRef:
                name: clawdevs-github-secret
                optional: true
            - secretRef:
                name: clawdevs-maton-secret
                optional: true
            - configMapRef:
                name: devops-worker-env
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            # SSH para operações git (SSH primeiro; HTTPS com $GITHUB_TOKEN como fallback)
            # Identidade Master Git (ClawDevs)
            - name: GIT_AUTHOR_NAME
              value: "Master DevOps (ClawDevs)"
            - name: GIT_AUTHOR_EMAIL
              value: "devops@clawdevs.ai"
            - name: GIT_COMMITTER_NAME
              value: "Master DevOps (ClawDevs)"
            - name: GIT_COMMITTER_EMAIL
              value: "devops@clawdevs.ai"
            # SSH para operações git (SSH primeiro; HTTPS com $GITHUB_TOKEN como fallback)
            # SSH para operações git (SSH configurado dinamicamente)
            - name: GITHUB_SSH_KEY_NAME
              valueFrom:
                secretKeyRef:
                  name: clawdevs-github-secret
                  key: GITHUB_SSH_KEY_NAME
                  optional: true
            - name: GIT_SSH_COMMAND
              value: 'ssh -i /root/.ssh/$(GITHUB_SSH_KEY_NAME) -o IdentitiesOnly=yes -o StrictHostKeyChecking=no'
            - name: GITHUB_USER
              valueFrom:
                secretKeyRef:
                  name: clawdevs-github-secret
                  key: GITHUB_USER
                  optional: true
            - name: GITHUB_ORG
              valueFrom:
                secretKeyRef:
                  name: clawdevs-github-secret
                  key: GITHUB_ORG
                  optional: true
          volumeMounts:
            - name: scripts
              mountPath: /config
              readOnly: true
            - name: ssh-setup
              mountPath: /root/.ssh
              readOnly: true
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: scripts
          configMap:
            name: devops-worker-scripts
            optional: true
        # EmptyDir partilhado entre initContainer (setup-ssh) e container principal
        - name: ssh-setup
          emptyDir: {}
