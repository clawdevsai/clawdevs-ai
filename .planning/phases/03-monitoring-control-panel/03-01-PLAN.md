---
phase: 03-monitoring-control-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - control-panel/backend/app/api/metrics.py
  - control-panel/backend/app/api/sessions.py
  - control-panel/backend/app/services/context_metrics.py
  - control-panel/backend/app/services/context_mode_metrics_broadcaster.py
  - control-panel/backend/tests/test_api/test_agents_sessions.py
  - control-panel/backend/tests/test_api/test_metrics.py
autonomous: true
requirements:
  - MON-01
  - MON-02
must_haves:
  truths:
    - "Control panel can query sessions from the last 30 minutes and full history."
    - "Control panel can query tokens consumed, backlog, tasks in progress, and tasks completed."
    - "Metrics updates can be pushed in real time."
  artifacts:
    - path: "control-panel/backend/app/api/sessions.py"
      provides: "Sessions list endpoint with window filtering."
    - path: "control-panel/backend/app/api/metrics.py"
      provides: "Overview metrics endpoint with MON-02 fields."
    - path: "control-panel/backend/app/services/context_mode_metrics_broadcaster.py"
      provides: "WS broadcast payload for monitoring metrics."
  key_links:
    - from: "control-panel/backend/app/api/sessions.py"
      to: "control-panel/backend/app/models/session.py"
      via: "SQLModel query"
      pattern: "Session"
    - from: "control-panel/backend/app/api/metrics.py"
      to: "control-panel/backend/app/services/context_metrics.py"
      via: "service call"
      pattern: "ContextMetrics"
    - from: "control-panel/backend/app/services/context_mode_metrics_broadcaster.py"
      to: "control-panel/backend/app/api/ws.py"
      via: "channel broadcast"
      pattern: "context-mode-metrics"
---

<objective>
Deliver session window filtering and baseline monitoring metrics (MON-01, MON-02) with real-time updates.

Purpose: Enable the control panel to show last-30-minute activity plus historical data and core metric cards.
Output: Updated sessions + metrics APIs and WS broadcast payload with MON-02 fields.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-monitoring-control-panel/03-CONTEXT.md
@.planning/phases/03-monitoring-control-panel/03-RESEARCH.md
@.planning/phases/03-monitoring-control-panel/03-UI-SPEC.md
@control-panel/backend/app/api/metrics.py
@control-panel/backend/app/api/sessions.py
@control-panel/backend/app/api/ws.py
@control-panel/backend/app/services/context_metrics.py
@control-panel/backend/app/services/context_mode_metrics_broadcaster.py

<interfaces>
From control-panel/backend/app/api/metrics.py:
```python
class OverviewMetrics(BaseModel): ...
@router.get("/overview", response_model=OverviewMetrics)
async def overview_metrics(...)
```

From control-panel/backend/app/api/sessions.py:
```python
class SessionsListResponse(BaseModel): ...
@router.get("", response_model=SessionsListResponse)
async def list_sessions(...)
```

From control-panel/backend/app/services/context_mode_metrics_broadcaster.py:
```python
class ContextModeMetricsBroadcaster: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session window filtering for last-30-min and history</name>
  <files>control-panel/backend/app/api/sessions.py</files>
  <read_first>
    control-panel/backend/app/api/sessions.py
    control-panel/backend/app/models/session.py
    control-panel/backend/app/core/database.py
  </read_first>
  <action>
    In `list_sessions`, add query param `window_minutes: int | None = 30`.
    - If `window_minutes` is `None`, return full historical list (no time filter).
    - If `window_minutes` is an int, accept only `{30, 60, 360, 1440}`; otherwise return `HTTPException(status_code=400)`.
    - Filter sessions where `Session.last_active_at >= now_utc - timedelta(minutes=window_minutes)`.
    - Always order by `Session.last_active_at` descending.
    - Keep response model `SessionsListResponse` unchanged (continue returning `sessions: list[SessionResponse]`).
  </action>
  <acceptance_criteria>
    - `control-panel/backend/app/api/sessions.py` contains `window_minutes: int | None = 30` in `list_sessions`.
    - `control-panel/backend/app/api/sessions.py` contains a filter on `Session.last_active_at` guarded by `window_minutes is not None`.
    - `control-panel/backend/app/api/sessions.py` orders by `Session.last_active_at.desc()`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api/test_agents_sessions.py -x</automated>
  </verify>
  <done>
    Sessions endpoint supports a 30-minute default window and full-history retrieval without response shape changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend overview metrics with MON-02 fields and WS payload</name>
  <files>
    control-panel/backend/app/api/metrics.py,
    control-panel/backend/app/services/context_metrics.py,
    control-panel/backend/app/services/context_mode_metrics_broadcaster.py
  </files>
  <read_first>
    control-panel/backend/app/api/metrics.py
    control-panel/backend/app/services/context_metrics.py
    control-panel/backend/app/services/context_mode_metrics_broadcaster.py
    control-panel/backend/app/models/task.py
    control-panel/backend/app/models/session.py
  </read_first>
  <action>
    Update `OverviewMetrics` to include these fields:
    - `tokens_consumed_total: int`
    - `tokens_consumed_avg_per_task: float`
    - `backlog_count: int`
    - `tasks_in_progress: int`
    - `tasks_completed: int`
    Add query param `window_minutes: int = 30` to `/metrics/overview`.
    Implement aggregation in `context_metrics.py` using:
    - `Task.status` to compute backlog (`inbox`), in-progress (`in_progress`), completed (`done`) counts within window.
    - `Session.token_count` summed within window for `tokens_consumed_total`.
    - `tokens_consumed_avg_per_task = tokens_consumed_total / max(tasks_completed, 1)`.
    Update `ContextModeMetricsBroadcaster` to include the new fields in the `context-mode-metrics` payload for real-time UI updates.
  </action>
  <acceptance_criteria>
    - `control-panel/backend/app/api/metrics.py` `OverviewMetrics` includes `tokens_consumed_total`, `tokens_consumed_avg_per_task`, `backlog_count`, `tasks_in_progress`, `tasks_completed`.
    - `control-panel/backend/app/api/metrics.py` `/overview` handler accepts `window_minutes: int = 30`.
    - `control-panel/backend/app/services/context_mode_metrics_broadcaster.py` payload includes the five MON-02 keys above.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api/test_metrics.py -x</automated>
  </verify>
  <done>
    Overview metrics endpoint returns MON-02 fields and WS broadcasts include the same fields.
  </done>
</task>

</tasks>

<verification>
Run backend tests for sessions + metrics and confirm no schema changes are pending.
</verification>

<success_criteria>
- Sessions endpoint supports last-30-minute filtering and full history.
- Overview metrics returns MON-02 fields and broadcasts them via WS.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitoring-control-panel/03-monitoring-control-panel-01-SUMMARY.md`
</output>
