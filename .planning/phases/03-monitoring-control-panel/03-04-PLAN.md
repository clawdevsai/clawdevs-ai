---
phase: 03-monitoring-control-panel
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - control-panel/frontend/src/app/monitoring/page.tsx
  - control-panel/frontend/src/components/monitoring/monitoring-tabs.tsx
  - control-panel/frontend/src/components/monitoring/metrics-cards.tsx
  - control-panel/frontend/src/components/monitoring/sessions-table.tsx
  - control-panel/frontend/src/components/monitoring/failure-panel.tsx
  - control-panel/frontend/src/lib/monitoring-api.ts
  - control-panel/frontend/src/app/settings/page.tsx
autonomous: true
requirements:
  - MON-01
  - MON-02
  - MON-03
  - MON-04
  - CTRL-01
must_haves:
  truths:
    - "CTO can view sessions, metrics, cycle time, throughput, and failures in a tabbed dashboard."
    - "CTO can update runtime settings from the control panel with confirmation prompts."
  artifacts:
    - path: "control-panel/frontend/src/app/monitoring/page.tsx"
      provides: "Tabbed monitoring dashboard UI."
    - path: "control-panel/frontend/src/lib/monitoring-api.ts"
      provides: "Client API calls for monitoring endpoints."
    - path: "control-panel/frontend/src/app/settings/page.tsx"
      provides: "Runtime settings editor UI."
  key_links:
    - from: "control-panel/frontend/src/app/monitoring/page.tsx"
      to: "control-panel/frontend/src/lib/monitoring-api.ts"
      via: "React Query fetchers"
      pattern: "useQuery"
    - from: "control-panel/frontend/src/app/monitoring/page.tsx"
      to: "control-panel/frontend/src/lib/ws.ts"
      via: "WS subscription"
      pattern: "context-mode-metrics"
    - from: "control-panel/frontend/src/app/settings/page.tsx"
      to: "control-panel/frontend/src/lib/monitoring-api.ts"
      via: "runtime settings calls"
      pattern: "updateRuntimeSettings"
---

<objective>
Build the monitoring dashboard UI and runtime settings editor aligned with the UI contract.

Purpose: Provide CTO-facing control panel views for monitoring and configuration.
Output: Tabbed dashboard UI + settings UI with confirmations.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-monitoring-control-panel/03-CONTEXT.md
@.planning/phases/03-monitoring-control-panel/03-RESEARCH.md
@.planning/phases/03-monitoring-control-panel/03-UI-SPEC.md
@control-panel/frontend/src/app/monitoring/page.tsx
@control-panel/frontend/src/app/settings/page.tsx
@control-panel/frontend/src/lib/ws.ts
@control-panel/frontend/src/lib/axios-instance.ts

<interfaces>
From control-panel/frontend/src/app/monitoring/page.tsx:
```tsx
interface ContextModeMetrics { ... }
const fetchMonitoringMetrics = () => ...
```

From control-panel/frontend/src/lib/ws.ts:
```ts
type WSChannel = "dashboard" | "agents" | "approvals" | "cluster" | "crons" | "context-mode-metrics";
export const wsManager = ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build tabbed monitoring dashboard UI (Sessions/Tasks/Agents/Metrics)</name>
  <files>
    control-panel/frontend/src/app/monitoring/page.tsx,
    control-panel/frontend/src/components/monitoring/monitoring-tabs.tsx,
    control-panel/frontend/src/components/monitoring/metrics-cards.tsx,
    control-panel/frontend/src/components/monitoring/sessions-table.tsx,
    control-panel/frontend/src/lib/monitoring-api.ts
  </files>
  <read_first>
    control-panel/frontend/src/app/monitoring/page.tsx
    control-panel/frontend/src/lib/axios-instance.ts
    control-panel/frontend/src/lib/query-client.ts
    control-panel/frontend/src/lib/ws.ts
    control-panel/frontend/src/components/ui/button.tsx
    control-panel/frontend/src/components/ui/badge.tsx
  </read_first>
  <action>
    Implement tabbed dashboard per D-01..D-04 with tabs: `Sessions`, `Tasks`, `Agents`, `Metrics`.
    Top row shows 4 cards: active sessions, tasks in progress, tokens consumed, failures.
    Use React Query fetchers in `monitoring-api.ts`:
    - `listSessions({ windowMinutes })` → `/sessions?window_minutes=30` and omit `window_minutes` (or pass `null`) for history.
    - `getOverviewMetrics({ windowMinutes })` → `/metrics/overview`.
    - `getCycleTime({ windowMinutes })` → `/metrics/cycle-time`.
    - `getThroughput({ windowMinutes, groupBy })` → `/metrics/throughput`.
    In `page.tsx`, subscribe to `context-mode-metrics` via `wsManager` and `queryClient.invalidateQueries` for `overview` and `sessions`.
    Apply UI contract:
    - Use colors: background `#080808`, panels `#0f0f0f`, accent `#00bfff`, destructive `#dc2828`.
    - Typography sizes: Body 16, Label 14, Heading 20, Display 28.
    - Copy: Empty state heading `No recent sessions` and body `Sessions will appear here once the runtime starts. Check back after your next run.`
  </action>
  <acceptance_criteria>
    - `control-panel/frontend/src/components/monitoring/monitoring-tabs.tsx` renders tab labels `Sessions`, `Tasks`, `Agents`, `Metrics`.
    - `control-panel/frontend/src/components/monitoring/metrics-cards.tsx` renders four cards with labels `Active sessions`, `Tasks in progress`, `Tokens consumed`, `Failures`.
    - `control-panel/frontend/src/lib/monitoring-api.ts` exports `listSessions`, `getOverviewMetrics`, `getCycleTime`, `getThroughput`.
    - `control-panel/frontend/src/app/monitoring/page.tsx` subscribes to `context-mode-metrics` via `wsManager`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir control-panel/frontend lint</automated>
  </verify>
  <done>
    Monitoring dashboard renders the tabbed layout and uses React Query + WS updates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add failure detail panel and metrics tables</name>
  <files>
    control-panel/frontend/src/app/monitoring/page.tsx,
    control-panel/frontend/src/components/monitoring/failure-panel.tsx
  </files>
  <read_first>
    control-panel/frontend/src/app/monitoring/page.tsx
    control-panel/frontend/src/components/monitoring/metrics-cards.tsx
  </read_first>
  <action>
    Add a `FailurePanel` component that:
    - Fetches `/tasks/{task_id}/failure` for selected failed task.
    - Shows summary by default with expand for stack trace + evidence list (D-09, D-10).
    In the `Tasks` tab, add a table section for cycle time + throughput:
    - Show `cycle_time_avg_seconds` and `cycle_time_p95_seconds`.
    - List throughput rows by `label` with `completed_count`.
  </action>
  <acceptance_criteria>
    - `control-panel/frontend/src/components/monitoring/failure-panel.tsx` renders `Message`, `Stack trace`, and `Evidence` sections.
    - `control-panel/frontend/src/app/monitoring/page.tsx` displays `cycle_time_avg_seconds` and `cycle_time_p95_seconds`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir control-panel/frontend lint</automated>
  </verify>
  <done>
    Failure details and cycle/throughput metrics are visible in the dashboard.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement runtime settings UI with confirmation prompts</name>
  <files>control-panel/frontend/src/app/settings/page.tsx</files>
  <read_first>
    control-panel/frontend/src/app/settings/page.tsx
    control-panel/frontend/src/components/ui/button.tsx
    control-panel/frontend/src/components/ui/tooltip.tsx
  </read_first>
  <action>
    Update settings page to call `GET /settings/runtime` and `PUT /settings/runtime` via `monitoring-api.ts`.
    Implement confirmation prompts:
    - For model changes: show prompt text `This will switch inference provider for all agents. Type CONFIRM to proceed.`
    - For disable agent: `This will remove the agent from active scheduling. Type DISABLE to proceed.`
    - For threshold change: `This may affect runtime stability. Type CONFIRM to proceed.`
    Primary CTA button text must be `Apply Runtime Settings`.
  </action>
  <acceptance_criteria>
    - `control-panel/frontend/src/app/settings/page.tsx` contains `Apply Runtime Settings`.
    - `control-panel/frontend/src/app/settings/page.tsx` contains `Type CONFIRM to proceed.` and `Type DISABLE to proceed.` strings.
    - `control-panel/frontend/src/app/settings/page.tsx` calls `updateRuntimeSettings` from `monitoring-api.ts`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --dir control-panel/frontend lint</automated>
  </verify>
  <done>
    Runtime settings can be updated from the UI with required confirmations.
  </done>
</task>

</tasks>

<verification>
Run frontend lint and visually spot-check tab layout and settings prompts.
</verification>

<success_criteria>
- Monitoring dashboard adheres to UI spec and shows all required data.
- Settings page updates runtime settings with confirmations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitoring-control-panel/03-monitoring-control-panel-04-SUMMARY.md`
</output>
