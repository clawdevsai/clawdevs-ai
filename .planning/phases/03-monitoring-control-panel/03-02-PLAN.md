---
phase: 03-monitoring-control-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - control-panel/backend/app/api/metrics.py
  - control-panel/backend/app/api/tasks.py
  - control-panel/backend/app/services/failure_detector.py
  - control-panel/backend/app/services/task_metrics.py
  - control-panel/backend/tests/test_api/test_metrics.py
  - control-panel/backend/tests/test_api/test_tasks.py
autonomous: true
requirements:
  - MON-03
  - MON-04
must_haves:
  truths:
    - "Control panel can query cycle time per task and throughput per team."
    - "Control panel can view failure details with message, stack trace, and evidence."
  artifacts:
    - path: "control-panel/backend/app/api/metrics.py"
      provides: "Cycle time + throughput endpoints."
    - path: "control-panel/backend/app/services/task_metrics.py"
      provides: "Cycle time and throughput computations."
    - path: "control-panel/backend/app/api/tasks.py"
      provides: "Failure detail endpoint."
  key_links:
    - from: "control-panel/backend/app/api/metrics.py"
      to: "control-panel/backend/app/services/task_metrics.py"
      via: "service call"
      pattern: "TaskMetricsService"
    - from: "control-panel/backend/app/api/tasks.py"
      to: "control-panel/backend/app/services/failure_detector.py"
      via: "failure lookup"
      pattern: "FailureDetector"
---

<objective>
Implement cycle time + throughput metrics and failure observability endpoints with tests (MON-03, MON-04).

Purpose: Provide backend data for cycle time/throughput and failure evidence panels.
Output: Metrics endpoints + failure endpoint + test coverage.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-monitoring-control-panel/03-CONTEXT.md
@.planning/phases/03-monitoring-control-panel/03-RESEARCH.md
@control-panel/backend/app/api/metrics.py
@control-panel/backend/app/api/tasks.py
@control-panel/backend/app/services/failure_detector.py
@control-panel/backend/app/models/task.py
@control-panel/backend/app/models/activity_event.py

<interfaces>
From control-panel/backend/app/api/metrics.py:
```python
@router.get("", response_model=MetricsListResponse)
async def list_metrics(...)
@router.get("/overview", response_model=OverviewMetrics)
async def overview_metrics(...)
```

From control-panel/backend/app/api/tasks.py:
```python
@router.get("/{task_id}/timeline", response_model=TaskTimelineResponse)
async def task_timeline(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing tests for cycle time, throughput, and failure detail</name>
  <files>
    control-panel/backend/tests/test_api/test_metrics.py,
    control-panel/backend/tests/test_api/test_tasks.py
  </files>
  <read_first>
    control-panel/backend/tests/test_api/test_metrics.py
    control-panel/backend/tests/test_api/test_tasks.py
    control-panel/backend/tests/conftest.py
  </read_first>
  <action>
    Add tests to `test_metrics.py`:
    - `test_cycle_time_metrics_windowed`: create tasks with `created_at`, `updated_at`, `status=done`, and assert `cycle_time_avg_seconds` and `cycle_time_p95_seconds` returned by `GET /metrics/cycle-time?window_minutes=30`.
    - `test_throughput_metrics_by_label`: create tasks with `label` values and assert `GET /metrics/throughput?window_minutes=30&group_by=label` returns per-label counts.
    Add tests to `test_tasks.py`:
    - `test_task_failure_detail`: create a failed task and matching `ActivityEvent`/failure evidence, assert `GET /tasks/{id}/failure` returns `message`, `stack_trace`, and `evidence` fields.
  </action>
  <acceptance_criteria>
    - `control-panel/backend/tests/test_api/test_metrics.py` contains `test_cycle_time_metrics_windowed` and `test_throughput_metrics_by_label`.
    - `control-panel/backend/tests/test_api/test_tasks.py` contains `test_task_failure_detail`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api/test_metrics.py -x</automated>
  </verify>
  <done>
    Tests exist for cycle time, throughput, and failure detail endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement cycle time, throughput, and failure detail endpoints</name>
  <files>
    control-panel/backend/app/api/metrics.py,
    control-panel/backend/app/api/tasks.py,
    control-panel/backend/app/services/task_metrics.py,
    control-panel/backend/app/services/failure_detector.py
  </files>
  <read_first>
    control-panel/backend/app/api/metrics.py
    control-panel/backend/app/api/tasks.py
    control-panel/backend/app/services/failure_detector.py
    control-panel/backend/app/models/task.py
    control-panel/backend/app/models/activity_event.py
  </read_first>
  <action>
    Create `control-panel/backend/app/services/task_metrics.py` with `TaskMetricsService`:
    - `get_cycle_time(window_minutes: int) -> dict` returns `cycle_time_avg_seconds` and `cycle_time_p95_seconds` using `Task.created_at` → `Task.updated_at` for tasks with `status == "done"` in window.
    - `get_throughput(window_minutes: int, group_by: str = "label") -> list[dict]` returns `{group: str, completed_count: int}` grouped by `Task.label`.
    In `metrics.py`, add endpoints:
    - `GET /metrics/cycle-time` returning `CycleTimeResponse` with `cycle_time_avg_seconds`, `cycle_time_p95_seconds`, `window_minutes`.
    - `GET /metrics/throughput` returning `ThroughputResponse` with `window_minutes`, `group_by`, `items`.
    In `tasks.py`, add:
    - `TaskFailureResponse` with `message`, `stack_trace`, `evidence`.
    - `GET /tasks/{task_id}/failure` that uses `FailureDetector` + `ActivityEvent` to return detail (message + stack trace + evidence list).
  </action>
  <acceptance_criteria>
    - `control-panel/backend/app/api/metrics.py` contains `@router.get("/cycle-time"` and `@router.get("/throughput"`.
    - `control-panel/backend/app/services/task_metrics.py` defines `TaskMetricsService` with `get_cycle_time` and `get_throughput`.
    - `control-panel/backend/app/api/tasks.py` contains `@router.get("/{task_id}/failure"` and `TaskFailureResponse`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api/test_tasks.py -x</automated>
  </verify>
  <done>
    Cycle time, throughput, and failure detail endpoints return expected data and satisfy tests.
  </done>
</task>

</tasks>

<verification>
Run metrics and tasks API tests to confirm new endpoints.
</verification>

<success_criteria>
- Cycle time and throughput endpoints return windowed metrics.
- Failure detail endpoint returns message, stack trace, and evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitoring-control-panel/03-monitoring-control-panel-02-SUMMARY.md`
</output>
