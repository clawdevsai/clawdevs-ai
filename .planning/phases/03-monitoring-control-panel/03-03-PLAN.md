---
phase: 03-monitoring-control-panel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - control-panel/backend/app/models/runtime_setting.py
  - control-panel/backend/app/models/__init__.py
  - control-panel/backend/app/services/runtime_settings.py
  - control-panel/backend/app/api/settings.py
  - control-panel/backend/app/migrations/versions/20260406_runtime_settings.py
  - control-panel/backend/tests/test_api/test_settings.py
autonomous: true
requirements:
  - CTRL-01
must_haves:
  truths:
    - "CTO can update runtime settings via API without recreating features."
    - "Destructive changes require explicit confirmation and are audited."
  artifacts:
    - path: "control-panel/backend/app/models/runtime_setting.py"
      provides: "Runtime settings + audit models."
    - path: "control-panel/backend/app/api/settings.py"
      provides: "Runtime settings GET/PUT endpoints."
    - path: "control-panel/backend/app/services/runtime_settings.py"
      provides: "Settings read/write + audit logic."
  key_links:
    - from: "control-panel/backend/app/api/settings.py"
      to: "control-panel/backend/app/services/runtime_settings.py"
      via: "service calls"
      pattern: "RuntimeSettingsService"
    - from: "control-panel/backend/app/services/runtime_settings.py"
      to: "control-panel/backend/app/models/runtime_setting.py"
      via: "SQLModel session"
      pattern: "RuntimeSetting"
---

<objective>
Add persisted runtime settings management with audit and confirmation gates (CTRL-01).

Purpose: Allow CTO to safely update runtime flags, provider/model selection, and agent management.
Output: Settings models + service + API endpoints + tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-monitoring-control-panel/03-CONTEXT.md
@.planning/phases/03-monitoring-control-panel/03-RESEARCH.md
@control-panel/backend/app/api/settings.py
@control-panel/backend/app/core/config.py
@control-panel/backend/app/models/user.py

<interfaces>
From control-panel/backend/app/api/settings.py:
```python
@router.get("/info")
async def get_settings_info(_: CurrentUser)
@router.get("/gateway-health")
async def get_gateway_health(_: CurrentUser)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime settings + audit models with migration</name>
  <files>
    control-panel/backend/app/models/runtime_setting.py,
    control-panel/backend/app/models/__init__.py,
    control-panel/backend/app/migrations/versions/20260406_runtime_settings.py
  </files>
  <read_first>
    control-panel/backend/app/models/__init__.py
    control-panel/backend/app/models/constants.py
    control-panel/backend/app/core/database.py
  </read_first>
  <action>
    Create `RuntimeSetting` and `RuntimeSettingAudit` SQLModel tables:
    - `RuntimeSetting`: `id`, `key`, `value_json`, `updated_at`, `updated_by_user_id`.
    - `RuntimeSettingAudit`: `id`, `setting_key`, `previous_value_json`, `new_value_json`, `action`, `confirm_text`, `created_at`, `created_by_user_id`.
    Add both models to `control-panel/backend/app/models/__init__.py`.
    Create Alembic migration `control-panel/backend/app/migrations/versions/20260406_runtime_settings.py` to create both tables with indexes on `key` and `setting_key`.
  </action>
  <acceptance_criteria>
    - `control-panel/backend/app/models/runtime_setting.py` defines `class RuntimeSetting` and `class RuntimeSettingAudit`.
    - `control-panel/backend/app/models/__init__.py` exports `RuntimeSetting` and `RuntimeSettingAudit`.
    - `control-panel/backend/app/migrations/versions/20260406_runtime_settings.py` contains `create_table("runtime_setting"` and `create_table("runtime_setting_audit"`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api/test_settings.py -x</automated>
  </verify>
  <done>
    Runtime settings tables exist with audit tracking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement runtime settings service + API with confirmation</name>
  <files>
    control-panel/backend/app/services/runtime_settings.py,
    control-panel/backend/app/api/settings.py,
    control-panel/backend/tests/test_api/test_settings.py
  </files>
  <read_first>
    control-panel/backend/app/api/settings.py
    control-panel/backend/app/core/config.py
    control-panel/backend/app/models/runtime_setting.py
    control-panel/backend/tests/test_api/test_settings.py
  </read_first>
  <action>
    Create `RuntimeSettingsService` with:
    - `get_settings()` returns current settings merged from DB overrides + env defaults.
    - `update_settings(payload, actor)` persists overrides and writes `RuntimeSettingAudit`.
    Add API endpoints:
    - `GET /settings/runtime` → returns settings JSON.
    - `PUT /settings/runtime` → accepts payload with keys:
      `limits`, `flags`, `model_provider`, `model_name`, `agent_updates`, `thresholds`, `confirm_text`.
    Confirmation rules (D-13):
    - If `model_provider` or `model_name` changes → require `confirm_text == "CONFIRM"`.
    - If any agent is disabled in `agent_updates` → require `confirm_text == "DISABLE"`.
    - If `thresholds` change → require `confirm_text == "CONFIRM"`.
    Tests in `test_settings.py`:
    - `test_get_runtime_settings` returns merged defaults.
    - `test_update_runtime_settings_requires_confirm` enforces confirm rules.
    - `test_update_runtime_settings_audits_change` verifies audit row created.
  </action>
  <acceptance_criteria>
    - `control-panel/backend/app/api/settings.py` contains `@router.get("/runtime")` and `@router.put("/runtime")`.
    - `control-panel/backend/app/services/runtime_settings.py` defines `RuntimeSettingsService`.
    - `control-panel/backend/tests/test_api/test_settings.py` contains `test_get_runtime_settings`, `test_update_runtime_settings_requires_confirm`, and `test_update_runtime_settings_audits_change`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_api/test_settings.py -x</automated>
  </verify>
  <done>
    Runtime settings can be read/updated with confirmation gates and audits.
  </done>
</task>

</tasks>

<verification>
Run settings API tests and confirm migration file exists.
</verification>

<success_criteria>
- Runtime settings are persisted and auditable.
- Destructive changes require explicit confirmation text.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitoring-control-panel/03-monitoring-control-panel-03-SUMMARY.md`
</output>
